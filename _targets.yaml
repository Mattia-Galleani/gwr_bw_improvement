main:
  main: |-
    library(targets) library(tarchetypes) tar_source() tar_option_set(packages = c("readr", "dplyr", "ggplot2"))
    list( tar_target(file, "data.csv", format = "file"), tar_target(raw_data, get_data(file)), tar_target(clean_data, clean_data(raw_data)), tar_target(model, fit_model(data)), tar_target(export,export_model(model)), tar_target(plot, plot_model(model, data)) )
